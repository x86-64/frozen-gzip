{ class = "thread" },

{ class = "assign", before = {
	file = (gzip_t)"examples/aaaa.gz",
	tmp  = (raw_t)""
}},

{ class = "query", data = (env_t)"file", request = {
	action      = (action_t)"convert_to",
	destination = (fd_t)"stdout"
}},

{ class = "kill" }
